{
  "name": "HelloWorld",
  "private": true,
  "version": "0.0.0",
  "description": "Hello World",
  "main": "server.js",
  "author": {
    "name": "",
    "email": ""
  },
  "dependencies": {
    "express": "^4.13.3",
    "mocha": "^3.0.0"
  },
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-mocha": "^2.2.0",
    "mocha-junit-reporter": "^1.12.0", //my change added comma
    "gulp-istanbul": "^1.1.3", //my change
    "nyc": "^12.0.2" //my change
  },
  "scripts": {
    "test": "nyc --reporter=cobertura --reporter=html ./node_modules/.bin/mocha tests/**/*.js --reporter mocha-junit-reporter --reporter-options mochaFile=./test-results.xml" //my change
  }
}
